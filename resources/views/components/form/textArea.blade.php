<div class="form-group">
    <label for="{{$name}}">Descrição</label>
    <textarea id="{{$name}}" name="{{$name}}" class="form-control" placeholder="Digite a descrição da task" rows="4">{{$value ?? ''}}</textarea>
</div>
